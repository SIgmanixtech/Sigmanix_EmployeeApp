import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.sigmanix.employee",
  appName: "Sigmanix Employee",

  server: {
    url: "https://sigmanix-employee-app.vercel.app",
  },
};

export default config;